select sysdate from dual;
select to_char(sysdate ,'mm-dd-yyyy') from dual;
select to_char(sysdate, "HH24:MI:SS")from dual;
select to_char(sysdate-1,'day-month ddth yyyy')"yesterday"from dual;
select abs(to_date('31-08-2023','dd-mm-yyyy')-to_date('01-06-23','dd-mm-yyyy'))"different"from dual;
